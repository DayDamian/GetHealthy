<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gethealthy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GetHealthy"
        tools:targetApi="31">
        <activity
            android:name=".CreateProductActivity"
            android:exported="false" />
        <activity
            android:name=".authentication.ManageUserActivity"
            android:exported="false" />
        <activity
            android:name=".authentication.EmailPasswordActivity"
            android:exported="true">
            android:label="@string/label_emailpassword"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".authentication.BaseActivity"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>